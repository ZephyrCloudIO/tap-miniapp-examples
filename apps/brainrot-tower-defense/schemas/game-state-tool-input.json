{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Brainrot Tower Defense game-state tool input",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "sessionId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Optional assertion for the current projected game session in the mounted TAP channel. Omit it to read the current game."
    }
  }
}
