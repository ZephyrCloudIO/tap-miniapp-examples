{
  "name": "tap-miniapp-examples",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "build:watch": "turbo run build:watch",
    "clean": "turbo run clean",
    "dev": "turbo run dev --parallel",
    "test": "pnpm run test:unit && pnpm run verify:tap",
    "test:unit": "turbo run test",
    "test:tap:static": "node --test scripts/tap-manifest-consent.test.mjs scripts/tap-package-policy.test.mjs scripts/tap-test-discovery.test.mjs scripts/tap-test-policy.test.mjs scripts/tap-test-runtime.test.mjs && node scripts/verify-tap-tests.mjs",
    "typecheck": "turbo run typecheck",
    "typecheck:tap": "turbo run typecheck:tap --filter=./apps/*",
    "verify": "pnpm run typecheck && pnpm run test",
    "verify:roadie:loop": "node scripts/verify-roadie-loop.mjs",
    "verify:tap": "pnpm run test:tap:static && pnpm run typecheck:tap"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "turbo": "2.10.5",
    "typescript": "^6.0.3"
  }
}
